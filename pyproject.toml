[tool.poetry]
name = "p-drought-indices"
version = "0.1.0"
description = "\"A package to use Deep Learning models to forecast drought indices\""
authors = ["Riccardo7-DS <riccardo_dercole@yahoo.it>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
